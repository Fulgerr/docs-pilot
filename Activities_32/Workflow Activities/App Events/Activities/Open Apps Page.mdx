# Open Apps Page

# Description

Opens the specified page of a web app.

# Usage

PropertyValueDescriptionWhat page to openRequiredSelects the page to be opened.Show as a pop-over (modal)False (by default)Open the page as a pop-over inside the window.Show as a bottom sheetFalse (by default)Opens the page as a sheet at the bottom of the window.
