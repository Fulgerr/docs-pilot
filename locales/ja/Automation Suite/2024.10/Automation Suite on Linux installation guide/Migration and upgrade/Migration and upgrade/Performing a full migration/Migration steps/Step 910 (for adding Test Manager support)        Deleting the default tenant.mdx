# Step 910 (for adding Test Manager support):
        Deleting the default tenant

You must delete the DefaultTenant tenant that was automatically created in Automation Suite since it serves no purpose.To do that, take the following steps:

Switch back to old UI.Go to Tenants, find DefaultTenant and delete it.
